$kind: http-request
url: "{{baseUrl}}/v2/lvmCache/DeleteCacheLv"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: Cookie
    value: "{{Cookie}}"
    description: 不可为空，用户认证所需cookie
  - key: X-Csrf-Token
    value: "{{X-Csrf-Token}}"
    description: 不可为空，用户认证所需token
  - key: X-Curpass-Token
    value: "{{X-Curpass-Token}}"
    description: 不可为空，密码认证所需token
body:
  type: json
  content: "{\r

    \    \"lv_uuid\": \"wRNhI2-Va6I-G8jG-VfEf-YgiE-O91K-FOY2kl\"\r

    }"
settings:
  strictSSL: false
  followRedirects: true
order: 8000
