# 存储管理版本资产

本目录是当前业务中心，用于按系统版本和应用版本组织存储管理重构测试资产。

当前本机版本包：

- `7.0/1.0.057/storagemanager_1.0.057_amd64.deb`
- `7.0/1.0.065/storagemanager_1.0.065_amd64.deb`

`.deb` 安装包仅在本机保存，由根目录 `.gitignore` 排除，不提交 Git。后续测试设计、版本说明和非二进制资产可放在对应版本目录并正常提交。
