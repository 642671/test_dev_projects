# 拟开放存储只读接口测试资产（2026-08-05）

## 交付结果

- 独立 Excel：`接口测试用例/单个接口测试用例/存储资源拟开放只读接口测试用例.xlsx`
- Apifox 场景：`拟开放存储资源只读接口验证`（ID `8598784`）
- Apifox 分支：`ai/20260805-from-main-storage-reconcile`（ID `8459871`）
- 场景状态：已创建并回读验证，尚未执行、尚未合并到 Apifox `main`

## 接口范围

| 顺序 | 接口 | 方法 | 路径 | 用途 | Apifox 接口 ID |
|---:|---|---|---|---|---:|
| 1 | 获取磁盘插槽总览 | GET | `/v2/disk/GetOverview` | 查看物理硬盘在位情况 | 484618536 |
| 2 | 获取磁盘分配总览 | GET | `/v2/storage/overview` | 查看存储池/卷/磁盘的结构 | 484618537 |
| 3 | 获取卷列表 | GET | `/v2/storage/list/volume` | 查看所有逻辑卷 | 484618549 |
| 4 | 获取存储池列表 | GET | `/v2/storage/list/storagePool` | 查看所有存储池 | 484618560 |
| 5 | 获取存储状态 | GET | `/v2/storage/status` | 快速判断是否有存储资源 | 484618552 |

## Excel 内容

- Sheet：`接口清单`、`概要`、`卷`、`存储池`
- 从权威工作簿按接口路径完整抽取 59 条用例：概要 18 条、卷 25 条、存储池 16 条
- 59 条用例的 A:O 单元格值与来源工作簿逐项一致
- XLSX ZIP 完整性检查通过

## Apifox 场景结构

场景包含 5 个 `http` 步骤，均为 `bindType=API`、`syncMode=MANUAL`，顺序与接口清单一致。

- 卷列表和存储池列表使用 `type=detail`
- 认证使用 `{{Cookie}}`、`{{X-Csrf-Token}}`
- 存储池列表步骤包含 HTTP 200 可视化断言
- 回读确认 5/5 步骤均有真实 `relatedId` 和正确的 `apiDetailId`

本场景可以直接选择指向存储管理重构分支的 Apifox 环境执行。由于本次未指定环境 ID，未自动运行，也不记录推测性的通过率。
