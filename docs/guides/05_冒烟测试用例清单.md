# TOS UI 自动化冒烟测试用例清单

> 最后更新：2026-06-04
> 用例总数：19 个（TOS 冒烟用例）
> 运行命令：`pytest -m smoke -v -s`

---

## 一、登录模块（2 个用例）

文件：`ui_automation/testcases/smoke/test_tos_login.py`

| 编号 | 用例名称 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|
| TC_LOGIN_001 | 勾选"保持登录"进行登录 | 输入用户名test → 点击下一步 → 输入密码 → 勾选保持登录 → 点击下一步 | 登录成功，URL 变为 /#/desktop |
| TC_LOGIN_002 | 不勾选"保持登录"进行登录 | 输入用户名test → 点击下一步 → 输入密码 → 不勾选保持登录 → 点击下一步 | 登录成功，URL 变为 /#/desktop |

---

## 二、导航栏模块（7 个用例）

文件：`ui_automation/testcases/smoke/test_tos_navbar.py`

| 编号 | 用例名称 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|
| TC_NAVBAR_001 | 验证导航栏可见且包含图标 | 登录后检查顶部导航栏 | 导航栏可见，图标数量 > 0 |
| TC_NAVBAR_002 | 悬浮识别并点击"开始" | 遍历导航栏悬浮 → 找到"开始" → 单击 | 开始菜单打开 |
| TC_NAVBAR_003 | 悬浮识别并点击"所有应用" | 遍历导航栏悬浮 → 找到"所有应用" → 单击 | 所有应用面板打开 |
| TC_NAVBAR_004 | 悬浮识别并点击"存储管理" | 遍历导航栏悬浮 → 找到"存储管理" → 单击 | 存储管理窗口打开 |
| TC_NAVBAR_005 | 悬浮识别并点击"终端" | 遍历导航栏悬浮 → 找到"终端" → 单击 | 终端窗口打开 |
| TC_NAVBAR_006 | 悬浮识别并点击"安全顾问" | 遍历导航栏悬浮 → 找到"安全顾问" → 单击 | 安全顾问窗口打开 |
| TC_NAVBAR_007 | 悬浮识别并点击"控制面板" | 遍历导航栏悬浮 → 找到"控制面板" → 单击 | 控制面板窗口打开 |

---

## 三、桌面右键菜单模块（3 个用例）

文件：`ui_automation/testcases/smoke/test_tos_desktop_menu.py`

| 编号 | 用例名称 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|
| TC_DESKTOP_001 | 桌面右键弹出菜单 | 在桌面空白区域右键 | 弹出菜单，包含"刷新"和"用户设置" |
| TC_DESKTOP_002 | 右键菜单点击"刷新" | 右键 → 点击"刷新" | 桌面图标刷新，菜单消失 |
| TC_DESKTOP_003 | 右键菜单点击"用户设置" | 右键 → 点击"用户设置" | 用户设置界面打开 |

---

## 四、用户设置模块（7 个用例）

文件：`ui_automation/testcases/smoke/test_tos_user_settings.py`

| 编号 | 用例名称 | 操作步骤 | 预期结果 |
|------|---------|---------|---------|
| TC_SETTINGS_001 | 用户设置界面加载 | 右键打开用户设置 → 检查界面 | 用户设置界面加载完成 |
| TC_SETTINGS_002 | 左侧导航模块验证 | 获取左侧导航列表 | 包含"账号"和"显示"两个模块 |
| TC_SETTINGS_003 | 账号模块 Tab 完整性 | 获取 Tab 标签列表 | 包含"用户信息"、"账号安全"、"其它" |
| TC_SETTINGS_004 | 切换到"显示"模块 | 点击左侧"显示"导航 | 成功切换到显示模块 |
| TC_SETTINGS_005 | 账号模块 Tab 切换 | 依次点击"账号安全"→"其它"→"用户信息" | 每次切换 Tab 后页面内容变化 |
| TC_SETTINGS_006 | 编辑用户信息并保存 | 输入描述/邮箱/电话 → 点击"应用" | 出现"设置成功!"提示 |
| TC_SETTINGS_007 | 其它 Tab 复选框操作 | 取消所有勾选→应用→验证成功；勾选所有→应用→验证成功 | 两次操作都出现"操作成功!"提示 |

---

## 运行方式

```bash
# 激活环境
cd /Users/miaoqi/Desktop/test_dev_projects
source venv/bin/activate

# 运行全部冒烟测试
pytest -m smoke -v -s

# 只运行登录测试
pytest ui_automation/testcases/smoke/test_tos_login.py -v -s

# 只运行导航栏测试
pytest ui_automation/testcases/smoke/test_tos_navbar.py -v -s

# 只运行桌面右键菜单测试
pytest ui_automation/testcases/smoke/test_tos_desktop_menu.py -v -s

# 只运行用户设置测试
pytest ui_automation/testcases/smoke/test_tos_user_settings.py -v -s

# 查看 HTML 报告
open reports/html/report.html
```

---

## 更新记录

| 日期 | 变更内容 |
|------|---------|
| 2026-06-02 | 初始版本：登录(2) + 导航栏(7) + 桌面右键(3) = 12 个 TOS 冒烟用例 |
| 2026-06-04 | 新增用户设置模块(7)：界面加载验证 + 导航切换 + Tab切换 + 用户信息编辑保存 + 其它Tab复选框操作 |
