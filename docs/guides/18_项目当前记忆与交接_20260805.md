# 项目当前记忆与交接（2026-08-05）

> 本文档是当前项目状态和协作约定的最高优先级记忆。旧的交接文档、状态报告与本文冲突时，以本文为准。
>
> 工作区：`D:\test_dev_projects`；当前开发分支：`win`。

## 一、当前核心目标

当前重点是 **存储管理应用重构的测试工作**，包括测试资产整理、Excel 单接口用例质量、Apifox 同步和后续执行准备。

- NAS 测试环境不是当前文档整理的中心任务；设备状态由用户自行确认。
- `test_automation/` 和历史 Python 文件暂不作为当前优先事项。
- `storagemanager/` 是当前业务中心，但应用代码或安装包操作仍以用户明确授权的范围为准。
- `tos_api_cli_tester` 暂停使用，已归入本地停用项目归档。
- 存储管理安装包仅本地保留，不纳入 Git。

## 二、长期协作约定

### 2.1 Git

- 默认在 `win` 分支修改、验证、提交并推送 `origin/win`。
- 只有用户明确要求时，才把 Git `win` 同步或覆盖到 `main`。
- 本文业务状态基准：`b78abdd fix: normalize workbook line breaks`；最新文档提交以 `git log -1` 为准。
- `main` / `origin/main` 当前基准：`f9ff7271d67d8af5fb840cb2f8dfe574be31e9ef`。
- 不使用会丢失工作区修改的 `git reset --hard`、`git checkout --` 等操作。

### 2.2 Excel 与 WPS

- 唯一真源：`接口测试用例/单个接口测试用例/存储管理单接口测试用例.xlsx`。
- **以后不把原生 Excel 或 WPS 作为必要步骤。** 默认使用脚本读写、ZIP/XML 精确修改、逐单元格比较和质量检查。
- 只有用户明确要求观察或控制 Windows 应用时，才使用 WPS/Excel；界面操作必须通过受控的 Computer Use 工具。
- 修改工作簿前，应关闭正在打开该文件的 WPS/Excel 窗口，避免文件锁、缓存和自动保存冲突。
- WPS 即使只打开/关闭文件，也可能自动重写样式；提交前必须与 Git 基准比较并排除非预期样式变化。
- Excel 备份暂时全部保留，由用户以后自行删除。

### 2.3 Apifox

- 项目 ID：`8122217`。
- Apifox CLI、MCP 和 Codex Apifox 技能已经配置；访问令牌不得写入 Git、Markdown 或日志。
- 远端写入前必须：确定目标分支、读取 CLI Schema、验证完整载荷；写入后必须逐条回读验证。
- Apifox AI 分支：
  - 名称：`ai/20260805-from-main-storage-reconcile`
  - ID：`8459871`
  - 来源：Apifox `main`（ID `7879358`）
- **未经用户新的明确确认，不得把该 AI 分支合并到 Apifox `main`。**

### 2.4 当前不处理的内容

- 906 个断言缺口暂不处理。
- 不根据未实际执行的用例推测通过率、回填率或真实响应。
- 不主动执行格式化磁盘、删除池/卷、安全擦除、RAID 修改等破坏性 NAS 操作。

## 三、核心测试资产现状

### 3.1 Excel 总体数据

| 指标 | 当前值 |
|---|---:|
| Sheet | 8 |
| Excel 总用例 | 2,825 |
| 方法错误类、不可导入 | 121 |
| 可导入用例 | 2,704 |
| Apifox main 名称/存在性基线 | 2,704 |
| 名称/存在性缺失 | 0 |
| 断言缺口（暂缓） | 906 |

涉及 Sheet：概要、卷、存储池、磁盘、虚拟磁盘、USB设备、热备盘、HyperCache。

### 3.2 2026-08-05 对账修复

- Excel 修复 119 个单元格：
  - 86 个表单接口 Content-Type 改为 `application/x-www-form-urlencoded`；
  - 32 个无请求体 GET 接口移除 Content-Type；
  - `卷!H112` 的非法路径参数名修正为 `uuid`，非法值 `asd` 保留。
- Apifox AI 分支修复 202 个不同用例，覆盖 96 个接口。
- 202 个完整载荷全部通过 Schema 校验，写入后 202/202 回读一致。
- Git/Apifox `main` 均未在本次对账修复中合并。

详细结果：

- `docs/reports/Apifox_Excel完整对账_20260805.md`
- `docs/reports/Apifox_Excel修复结果_20260805.md`

### 3.3 换行清理

- 已把 6,710 个单元格、12,279 处非标准 CR/`_x000D_` 序列规范为 LF。
- 清理后含 CR 或 `_x000D_` 的单元格为 0。
- 仅修改 XLSX 内部 `xl/sharedStrings.xml`；样式、公式、合并单元格、冻结窗格均无差异。
- `卷!K126` 当前是 9 行正常文本，业务内容仍为 `enable: false`。
- 未使用 WPS 或原生 Excel 保存最终文件；WPS 自动产生的 369 个样式副作用已排除。

详细结果：`docs/reports/Excel换行清理结果_20260805.md`。

## 四、当前质量门禁

正式工具入口：`tools/api_case_pipeline/`。

```powershell
# 九维体检
node tools\api_case_pipeline\all_sheet_check.js

# Excel ↔ Apifox 名称/存在性检查
node tools\api_case_pipeline\reverse_check.js

# 完整内容对账
node tools\api_case_pipeline\reconcile_excel_apifox.js

# Excel 异常换行只读统计
python tools\api_case_pipeline\normalize_excel_linebreaks.py
```

最近验证结果：

- 九维体检：8 个 Sheet，总问题数 0。
- Excel 异常 CR/`_x000D_`：0。
- 工作簿可正常解析，ZIP 校验通过。
- 换行清理相对 Git 基准：6,710 个预期值差异，计划外值差异 0，样式差异 0，公式差异 0。

## 五、目录与资产原则

- `docs/`：正式文档唯一入口。
- `tools/api_case_pipeline/`：正式、可复用工具。
- `temp_scripts/`：Git 忽略的本地工作台、载荷、日志和可恢复备份，不作为正式入口。
- `archives/`：历史、停用和只读资产。
- `接口测试用例/`：当前测试设计真源与 Apifox 配套资产。
- `storagemanager/`：存储管理应用重构相关资产。
- `miaoqi/`：个人素材，禁止移动、修改或提交。

目录重建已经完成并提交；后续避免重新把一次性脚本、缓存、安装包或大量历史文件放回根目录。

## 六、下一步建议顺序

1. 用户在 Apifox 中审阅 AI 分支 `8459871` 的 202 个用例差异。
2. 用户明确确认后，才合并 Apifox AI 分支到 Apifox `main`。
3. 如需同步 Git 主分支，由用户明确要求后把 `win` 同步到 `main`。
4. 继续围绕存储管理重构确定测试范围、版本差异和执行批，不先处理 906 个断言缺口。
5. 实际执行接口后再回填响应、通过率与断言，不做推测。

## 七、新会话建议读取顺序

新 AI/Codex 会话开始时，按以下顺序读取：

1. `docs/guides/18_项目当前记忆与交接_20260805.md`（本文，最高优先级）
2. `docs/guides/01_项目目录说明.md`
3. `docs/guides/17_项目目录重建方案_20260804.md`
4. `docs/reports/Apifox_Excel修复结果_20260805.md`
5. `docs/reports/Excel换行清理结果_20260805.md`
6. `tools/api_case_pipeline/README.md`

## 八、记忆更新规则

- 每完成一个重要里程碑，更新本文的提交、分支、验证结果和下一步。
- 不在本文记录密码、Cookie、SSH 密码、Apifox Key 或其他令牌。
- 历史结论保留在 `docs/reports/`；本文只维护当前有效状态和长期约定。

---

最后更新：2026-08-05

当前 Git 分支：`win`

当前业务状态基准：`b78abdd fix: normalize workbook line breaks`
