# Excel、Apifox 单接口用例与通用场景最终核对（2026-08-06）

## 核对范围

- 项目：TEST-TNAS（8122217）
- Apifox 分支：main
- Excel：`接口测试用例/单个接口测试用例/存储管理单接口测试用例.xlsx`
- 核对方式：以“接口 ID + 用例标题”做单接口双向对账；以通用类型对应的真实单接口用例集合做场景覆盖对账。
- 本次未运行接口请求，未回填实际结果和断言执行结果。

## Excel ↔ Apifox 单接口用例

| 项目 | 数量 |
|---|---:|
| Excel 总用例 | 2825 |
| 错误方法类（按规则不导入单接口） | 121 |
| Excel 可导入用例 | 2704 |
| Apifox main 单接口用例 | 2704 |
| Excel 缺失于 Apifox | 0 |
| Apifox 额外用例 | 0 |
| 重复匹配键 | 0 |

结论：2704 条可导入用例一一对应，零差异。

## 通用场景最终覆盖

| 场景 | 场景 ID | 应覆盖 | 当前步骤 | 缺失 | 重名 |
|---|---:|---:|---:|---:|---:|
| 未登录 | 8558576 | 122 | 122 | 0 | 0 |
| Token 失效 | 8558577 | 122 | 122 | 0 | 0 |
| CSRF 不一致 | 8558579 | 122 | 122 | 0 | 0 |
| 无权限用户 | 8600045 | 122 | 122 | 0 | 0 |
| URL/版本异常 | 8552406 | 147 | 147 | 0 | 0 |
| 接口超时 | 8566372 | 122 | 122 | 0 | 0 |
| 错误方法 | 8564955 | 121 | 133 | 0 | 0 |

错误方法场景的 133 条中，121 条对应 Excel；另外 12 条为原场景已有的虚拟磁盘扩展步骤，本次保留，未删除。

## 结构和内容门禁

- 四类认证场景凭证矩阵不一致：0。
- 超时场景 122/122 均配置 1ms 超时。
- 错误方法场景 133/133 均保留方法错误断言。
- HTTP 绑定异常：0。
- 自定义请求关联异常：0。
- 场景标题重名：0。
- 新增前均保存完整备份；写入后均使用 `with-case-detail` 回读。
- 旧步骤除明确重建的错误步骤外保持不变。

## 本次补齐

- 未登录、Token 失效、CSRF 不一致、无权限：各新增 28 条。
- URL/版本异常：新增 28 条。
- 接口超时：新增 28 条。
- 错误方法：新增 28 条；保留 12 条历史扩展步骤。
- 修复热备盘新增认证步骤的凭证值。
- 重建“全部卷容量占用”和“碎片整理”的错误自定义步骤，统一真实路径和请求方法。

## 最终结论

Excel 可导入单接口用例与 Apifox main 已一一对应；7 个通用场景已覆盖当前全部对应单接口通用用例，缺失为 0。

