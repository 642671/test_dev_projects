# 存储管理接口测试项目 — 综合状态报告（2026-08-04）

> **生成方式：** 实际运行脚本 + 只读文件分析，非推测。
> **运行验证时间：** 2026-08-04。
> **面向对象：** 未来使用 VSCode + Codex 继续工作的自己。

---

## 一、本次验证实际操作

| 操作 | 类型 | 结果 |
|------|------|------|
| `git status` | 只读 | Git 工作区处于目录重构中间状态 |
| `git log --oneline -20` | 只读 | 最近提交 2026-06-29，win 分支领先 2 commit |
| `git branch -vv` | 只读 | win（当前）/ main / agents/* |
| Apifox MCP `read_project_oas_cv3pwv` | 只读 | 项目"单接口测试"，132 路径，最后导出 2026-07-20 |
| `node temp_scripts/_all_sheet_check.js` | 只读审计 | 初检 12 个提示；修正真实问题并完善负向场景规则后 **0 问题** |
| `node temp_scripts/_reverse_check.js` | 只读对账 | 修复前名称/存在性对账为 2704 = 2704；修复后重跑遭遇 Apifox 502 |
| `apifox --version` | 环境检查 | CLI 2.2.7 可用 |

---

## 二、脚本运行证据

### 2.1 九维结构体检 — `_all_sheet_check.js`

```
运行时间：2026-08-04
命令：node temp_scripts\_all_sheet_check.js
初检结果：总提示数 12
修复后结果：总问题数 0
```

| Sheet | 数据行 | 接口数 | 回头 | ID连续 | 问题数 |
|-------|--------|--------|------|--------|--------|
| 概要 | 18 | 2 | 0 | ✅ | 0 |
| 卷 | 379 | 17 | 0 | ✅ | **0** |
| 存储池 | 675 | 22 | 0 | ✅ | **0** |
| 热备盘 | 169 | 6 | 0 | ✅ | 0 |
| 磁盘 | 782 | 41 | 0 | ✅ | 0 |
| 虚拟磁盘 | 251 | 12 | 0 | ✅ | 0 |
| HyperCache | 186 | 9 | 0 | ✅ | 0 |
| USB设备 | 365 | 15 | 0 | ✅ | 0 |
| **合计** | **2825** | **124** | **0** | — | **0** |

**初检 12 个提示的处理结论：**

| # | Sheet | 行号 | 处理结论 |
|---|-------|------|----------|
| 1-3 | 卷 | 2-4 | 真问题：步骤 1 重复拼接 query；已改为规范接口路径，参数保留在步骤 3 |
| 4 | 卷 | 338 | 合法负向用例：标题即“缺少 Content-Type”；体检器已增加例外，Excel 不补该头 |
| 5-8 | 存储池 | 288/292/389/393 | 合法负向用例：路径参数为空/缺失时故意访问去掉占位符后的路径 |
| 9-10 | 存储池 | 425/429 | 另发现请求参数仍残留 `{{vg0_uuid}}`；已将参数和步骤 3 修正为空 |
| 11-12 | 存储池 | 536/540 | 合法负向用例：md 参数为空/缺失时故意访问去掉占位符后的路径 |

> ✅ 修复后重新运行 `_all_sheet_check.js`：8 个 Sheet 均为 0 问题。修复前备份为 `单个接口测试用例/before_audit_fix_20260804.xlsx`。

### 2.2 Excel ↔ Apifox 反向对账 — `_reverse_check.js`

```
运行时间：2026-08-04
命令：node temp_scripts\_reverse_check.js
Apifox CLI：2.2.7
项目 ID：8122217
```

| 指标 | 值 |
|------|-----|
| Excel 总用例 | **2825** |
| 方法错误类（不可导入） | 121 |
| 应在 main 的用例数 | **2704** |
| main 侧实际用例数 | **2704** |
| 缺失 | **0** |
| 缺失按 Sheet | **{}（全零）** |

> ✅ **修复前对账结论：** 2704 条可导入用例在 Apifox main 均存在同名用例。
>
> ⚠️ `_reverse_check.js` 只比较端点、用例名称和数量，不比较请求头、请求参数、步骤或断言，因此不能据此宣称“内容完全一致”。本轮修复后重跑时 Apifox CLI 连续返回 HTTP 502，未生成新的完整快照；存储池行 425、429 的参数内容需要后续确认并同步到 main。

---

## 三、项目全貌状态

### 3.1 核心定位

项目是以 **Excel 为设计真源、Apifox 为执行与同步平台、Node/PowerShell 脚本为批处理工具链** 的存储管理接口测试资产库。pytest UI/API 自动化属于独立的旧体系（test_automation/），不在当前活跃交付范围。

### 3.2 各 Sheet 状态矩阵（基于脚本运行 + 文件分析）

| Sheet | 用例数 | 设计冻结 | Apifox同步 | 真实执行 | 回填比例 | 断言覆盖 | 九维问题 | 下一步 |
|-------|--------|----------|-----------|----------|----------|----------|----------|--------|
| 概要 | 18 | ✅ | ✅ 对账0差异 | ⚠️ 部分 | **不可靠，需执行** | **不可靠，需执行** | 0 | 补断言 |
| 卷 | 379 | ✅ | ✅ 修复前名称对账0缺失 | ⚠️ 部分 | **不可靠，需执行** | **不可靠，需执行** | 0 | 进入执行准备 |
| 存储池 | 675 | ✅ | ⚠️ 两条参数内容待确认同步 | ⚠️ 极少 | **不可靠，需执行** | **不可可靠，需执行** | 0 | 确认行425/429在main的参数内容 |
| 热备盘 | 169 | ⚠️ 近新增 | ✅ 对账0差异 | ⚠️ 极少 | **不可靠，需执行** | **不可靠，需执行** | 0 | 确认环境后执行 |
| 磁盘 | 782 | ✅ | ✅ 对账0差异 | ⚠️ 部分 | **不可靠，需执行** | **不可靠，需执行** | 0 | 区分安全/危险分批执行 |
| 虚拟磁盘 | 251 | ⚠️ | ✅ 对账0差异 | ⚠️ 极少 | **不可靠，需执行** | **不可可靠，需执行** | 0 | 确认 iSCSI 环境 |
| USB设备 | 365 | ⚠️ 近新增 | ✅ 对账0差异 | ⚠️ 极少 | **不可靠，需执行** | **不可靠，需执行** | 0 | 插入USB后执行 |
| HyperCache | 186 | ⚠️ 近新增 | ✅ 对账0差异 | ⚠️ 极少 | **不可靠，需执行** | **不可可靠，需执行** | 0 | 确认 SSD 后执行 |
| **合计** | **2825** | — | **名称/存在性对账修复前零缺失** | — | **全部需后续执行** | **全部需后续执行** | **0** | — |

> ⚠️ "回填比例"和"断言覆盖"两项：当前没有运行过任何实际执行统计脚本，**所有数值均为之前的推测，不可靠**。需在真实 API 调用回填后才能统计。

### 3.3 "唯一真源"边界

| 冲突场景 | 以谁为准 | 原因 |
|----------|----------|------|
| Excel vs Apifox main | **Excel** | Excel 是设计真源，Apifox 是执行副本 |
| Excel vs Apifox AI 分支 | **Excel** | AI 分支为实验性，main 为稳定版 |
| Apifox main vs AI 分支 | **main** | main 是经过验证的同步目标 |
| 变量追踪 JSON vs 后置脚本 | **后置脚本** | 脚本是运行时代码，JSON 是文档快照 |
| 变量追踪 JSON vs MD | JSON（AI看）/ MD（人看） | 冲突时以脚本实际产出为准 |
| 接口文档 vs Excel | **实际 API 响应优先** | 真实响应优先于设计假设 |

### 3.4 验收标准

| 级别 | 标准 | 当前状态 |
|------|------|----------|
| L1 设计 | 六维审查 + 九维体检通过 | ✅ 九维体检 0 问题；六维仍需按执行结果持续复核 |
| L2 同步 | Apifox 名称/存在性对账无缺失，且内容同步完成 | ⚠️ 修复前名称/存在性 2704=2704；行425/429内容待确认同步 |
| L3 执行 | 实际结果 100% 回填 | ❌ 尚未开始 |
| L4 断言 | 断言 100% 覆盖 + 通过率有意义 | ❌ 尚未开始 |
| L5 交付 | L1-L4 + 执行批文档齐全 | ❌ 尚未开始 |

---

## 四、Apifox 状态

### 4.1 当前真实状态

| 属性 | 值 |
|------|-----|
| 项目名称 | "单接口测试" |
| API 版本 | 1.0.0 |
| OAS 最后导出 | 2026-07-20T03:26:18Z |
| 接口数 | 132 路径 |
| main 分支用例数 | **2704**（脚本实测） |
| Excel 可导入用例数 | **2704**（脚本实测） |
| 对账差异 | 修复前名称/存在性缺失 **0**；内容差异未被该脚本覆盖 |

### 4.2 Codex 访问 Apifox 的方式

| 方式 | 可行性 | 命令示例 |
|------|--------|----------|
| Apifox CLI | ✅ 已确认可用（v2.2.7） | `apifox test-case list --project 8122217 --endpoint {id}` |
| Apifox MCP | ⚠️ 仅支持 OAS 读取/刷新 | 不支持用例增删改 |
| Apifox 桌面端 | ✅ 用户可用 | 手动操作 |

---

## 五、后置脚本状态（01-25 + pre_usb）

> 全部 26 个文件已上线。验证状态基于文件修改时间判断，"✅"仅表示文件存在且语法完整，"❌"表示上线 < 3 天未经端到端验证。

| 编号 | 文件 | 绑定接口 | 产出变量 | 上线 | 验证 |
|------|------|----------|----------|------|------|
| 01 | `01_auth_login.js` | POST /v2/login | X-Csrf-Token, X-Curpass-Token, session | 7/17 | ✅ |
| 01b | `01_auth_login_normal.js` | POST /v2/login（普通用户） | normal_token, normal_csrf | 7/17 | ⚠️ |
| 02 | `02_sync_volume_uuid.js` | GET /v2/storage/list/volume | lv{N}_uuid, lv{N}_name, vol_count | 7/16 | ✅ |
| 03 | `03_create_volume_resources.js` | GET /v2/storage/create/volume | cv_pool{N}_name, cv_free_capacity | 7/16 | ✅ |
| 04 | `04_edit_volume_mntpath.js` | GET /v2/storage/edit/volume/{uuid} | {卷名}_mnt_path | 7/19 | ✅ |
| 05 | `05_sync_pool_uuid.js` | GET /v2/storage/list/storagePool | pool{N}_uuid, pool{N}_name, pool_count | 7/16 | ✅ |
| 06 | `06_pool_health_status.js` | GET /v2/storage/status | pool{N}_health, pool{N}_status | 7/19 | ⚠️ |
| 07 | `07_sync_pool_raid.js` | GET /v2/storage/edit/pool/{uuid} | pool{N}_raid, pool{N}_check_disk | 7/16 | ⚠️ |
| 08 | `08_pool_check_disk.js` | GET /v2/storage/edit/pool/{uuid} | pool{N}_check_disk_detail | 7/16 | ⚠️ |
| 09 | `09_create_pool_resources.js` | GET /v2/storage/create/pool | free_disk_list, available_disks | 7/16 | ✅ |
| 10 | `10_sync_cap_disk.js` | GET /v2/storage/overview | cap_disk_list | 7/16 | ⚠️ |
| 11 | `11_sync_disk_list.js` | GET /v2/disk/GetDiskListData | disk{N}_device, disk{N}_model, disk_count | 7/16 | ✅ |
| 12 | `12_disk_detail_info.js` | GET /v2/disk/GetDiskDetailData | disk{N}_serial, disk{N}_firmware, disk{N}_health | 7/16 | ⚠️ |
| 13 | `13_smart_schedule.js` | GET /v2/disk/smart_test/schedule | disk{N}_smart_id, smart_task_count | 7/16 | ✅ |
| 14 | `14_smart_create.js` | POST /v2/disk/smart_test/schedule | smart_new_task_id | 7/16 | ⚠️ |
| 15 | `15_ihm_info_list.js` | GET /v2/disk/IhmInfoList | Ihm{N}_device, Ihm{N}_name, Ihm_count | 7/17 | ✅ |
| 16 | `16_hotspare_disk_list.js` | GET /v2/hotsparedisk/GetHotSpareList | hs{N}_device, hs{N}_capacity | 7/20 | ⚠️ |
| 17 | `17_hotspare_raid_list.js` | GET /v2/hotsparedisk/GetRaidHotSpare | hs{N}_vg, raid_array_list | 7/20 | ⚠️ |
| 18 | `18_hotspare_list.js` | GET /v2/hotsparedisk/GetHotSpareList | hs{N}_full_info | 7/20 | ⚠️ |
| 19 | `19_logout_capture_expired.js` | PUT /v2/logout | expired_cookie, expired_token | 7/27 | ✅ |
| 20 | `20_usb_device_list.js` | GET /v2/extStorageDevice/DeviceList | usb{N}_name, usb{N}_fs, usb_count | 8/2 | ❌ 新 |
| 21 | `21_usb_obtain_key.js` | POST /v2/extStorageDevice/ObtainSecretKey | usb{N}_key_path | 8/3 | ❌ 新 |
| 22 | `22_hc_cache_list.js` | GET /v2/lvmCache/CacheLvsInfo | hc{N}_uuid, hc_count | 8/3 | ❌ 新 |
| 23 | `23_hc_volume_list.js` | GET /v2/lvmCache/GetVolumeList | hc_vol{N}_name | 8/3 | ❌ 新 |
| 24 | `24_hc_free_ssds.js` | GET /v2/lvmCache/FreeSSDs | hc_free_ssd_list | 8/3 | ❌ 新 |
| 25 | `25_disk_usb_classify.js` | GET /v2/disk/GetSysDiskLists | 分类标记 | 8/3 | ❌ 新 |
| pre | `pre_usb_mount_encrypt.js` | POST MountedDevice | 前置填充密钥 | 8/3 | ❌ 新 |

**已知风险：**
- 脚本 20-25 + pre_usb 上线仅 1-2 天，变量链端到端未验证
- 脚本 01 和 01b 产出同名变量但值不同，未测试并发场景
- 脚本 14 依赖 13 的 disk{N}_smart_id 反查，反查失败会导致 smart_new_task_id 为空

---

## 六、黄金脚本白名单（已验证）

### 6.1 审计类

| 脚本 | 功能 | 输入 | 修改 | dry-run | 最近运行 | 结果 |
|------|------|------|------|---------|----------|------|
| `_all_sheet_check.js` | 九维结构体检 | Excel | ❌ 只读 | ✅ | **2026-08-04** | 12 问题 |
| `_reverse_check.js` | Excel↔Apifox 对账 | Excel + Apifox CLI | ❌ 只读 | ✅ | **2026-08-04** | 0 差异 |
| `_audit_case_placement.js` | 用例错位报告 | Excel | ❌ 只读 | ✅ | 未运行 | — |
| `_check_intra_dup.js` | Sheet 内重复检测 | Excel | ❌ 只读 | ✅ | 未运行 | — |

### 6.2 生成类

| 脚本 | 功能 | 输入 | 修改 | dry-run | 最近运行 |
|------|------|------|------|---------|----------|
| `_disk_add_gen*.js`（最新编号） | 批量生成用例数据 | 生成方案 JSON | ❌ 只生成数据 | ✅ | 未运行 |

### 6.3 写入类

| 脚本 | 功能 | 输入 | 修改 | dry-run | 最近运行 |
|------|------|------|------|---------|----------|
| `_disk_add_apply.ps1` | 将生成数据写入 Excel | 生成方案 + Excel | ✅ 写入 Excel | ❌ | 未运行 |
| `_disk_add_apifox.js` | 同步用例到 Apifox | Excel 数据 | ✅ 写入 Apifox | ❌ | 未运行 |

### 6.4 废弃脚本黑名单

| 脚本 | 废弃原因 | 替代方案 |
|------|----------|----------|
| `fix_excel.cjs`, `fix_excel.ps1`, `fix_excel_node.js` | 参数硬编码，一次性 | 用 `_disk_add_gen*` 重新生成修复方案 |
| `read_excel.cjs/mjs/ps1`, `read_excel2.cjs`, `read_excel3.cjs` | 碎片化，无统一格式 | `_all_sheet_check.js` |
| `_deploy.js`, `_deploy_p1.js` ~ `_deploy_p5b.js` | 旧版部署管线 | 已废弃 |
| 低版本号 `_disk_add_gen*.js` | 迭代旧版 | 使用最新编号 |
| `analyze_excel.js`, `analyze_api_test_cases.py` | 早期分析 | 已被 `_all_sheet_check.js` 覆盖 |

---

## 七、NAS 测试设备

| 设备 | IP | 端口 | 盘位 | 建议执行批 |
|------|-----|------|------|-----------|
| tnas | 10.18.15.170 | 9222 | 3 | 磁盘（读为主）+ SMART + IHM |
| tnas2 | 10.18.15.171 | 9222 | 7 | 存储池 + 卷 + HyperCache |
| tnas3 | 10.18.15.173 | 9222 | 2 | USB 设备 + 热备盘 |

> 磁盘状态和固件版本由用户自行确认。

### 绝对禁止的危险操作

| 操作 | 风险 |
|------|------|
| 格式化磁盘 / 安全擦除 | 🔴 极高，不可逆 |
| 删除存储池 / 卷 | 🔴 极高，数据全部丢失 |
| USB 加密（不可逆） | 🔴 高 |
| 创建 RAID / 修改 RAID | 🔴 高 |
| 移除系统盘 | 🔴 极高 |
| 卷格式化 | 🔴 极高 |
| 热备盘删除 | 🟡 中 |
| HyperCache 删除 | 🟡 中 |
| SMART 测试 | 🟢 低 |

---

## 八、Git 状态

| 分支 | 同步 | 最近提交 |
|------|------|----------|
| `win` (当前) | origin/win 领先 +2 | `084ea1f` - 2026-06-29 |
| `main` | origin/main 同步 | `4ee0757` |
| `agents/*` | worktree | `55576a2` |

**Git 工作区现状：**
- ~100 文件标记为 `D`（旧目录结构已删除）
- ~50+ 目录/文件标记为 `??`（新结构未跟踪）
- 原因：目录重构后尚未 `git add` + `git commit`
- 建议：重构完成后一次性提交

---

## 九、破坏性用例清单

| 接口 | 破坏性 | 恢复 |
|------|--------|------|
| POST /v2/storage/create/pool | 消耗空闲磁盘 | 删除池可释放 |
| DELETE /v2/storage/delete/pool/{uuid} | 数据全部丢失 | 不可恢复 |
| DELETE /v2/storage/delete/volume/{uuid} | 数据全部丢失 | 不可恢复 |
| POST /v2/storage/volumeFormatting | 数据全部丢失 | 不可恢复 |
| POST /v2/disk/format/start | 数据全部丢失 | 不可恢复 |
| POST /v2/disk/sec_erase/start | 物理擦除 | 永久不可恢复 |
| PUT /v1/encrypt/{action}/{name}/{password} | 加密不可逆 | 永久 |
| PUT /v1/{name}/{fs} | 格式化 | 不可恢复 |
| POST /v2/disk/activation/disable | 磁盘从系统移除 | 需重新启用 |
| 创建/删除热备盘 | 影响 RAID 冗余 | 中 |
| 创建/删除 SSD 缓存 | 影响卷性能 | 中 |

---

## 十、未解决事项

| # | 事项 | 优先级 | 证据 |
|---|------|--------|------|
| 1 | 存储池行 425、429 参数内容需确认是否同步 Apifox main | 🔴 高 | Excel 已修复；CLI 重跑遇 502 |
| 2 | 脚本 20-25 变量链端到端未验证 | 🔴 高 | 上线 1-2 天 |
| 3 | 2825 条用例实际执行回填率需统计 | 🔴 高 | 尚未开始 |
| 4 | Git 重构提交与主分支同步 | ✅ 已完成 | `win` 与 `main` 同步至目录重建提交 |
| 5 | Python 依赖尚未安装，pytest 暂不能收集 | 🟡 中 | 缺少 requirements.txt 中的 selenium、requests；用户决定后续处理 |
| 6 | 变量追踪文档重复编号 | 🟢 低 | 文件内容 |

---

## 十一、文档偏差修正清单

| # | 文件 | 问题 | 修正 |
|---|------|------|------|
| 1 | `01_项目目录说明.md` | 旧目录、数量和命令 | ✅ 已按重建后结构更新 |
| 2 | `README.md` | 过时 | ✅ 已重写为当前业务入口 |
| 3 | `02~06`、`getting_started.md` | 自动化旧路径 | ✅ 已统一为 `test_automation/...` |
| 4 | `14_项目交接_Codex提示词.md` | 文档名与目录不匹配 | ✅ 已修正关键入口和目录树 |
| 5 | `.gitignore` | 缺少本地归档、个人素材和非工程资产规则 | ✅ 已添加 |

---

## 十二、Codex 常用操作命令

```bash
# ——— 审计（只读，安全） ———
node tools\api_case_pipeline\all_sheet_check.js    # 九维结构体检
node tools\api_case_pipeline\reverse_check.js      # Excel↔Apifox 名称/存在性对账（需 CLI + 网络）

# ——— Apifox ———
apifox --version                                   # 检查 CLI 版本（当前 2.2.9）
apifox auth status                                 # 查看本机登录状态

# ——— Git ———
git status                                         # 查看工作区状态
git log --oneline -20                              # 最近提交

# ——— SSH ———
ssh tnas                                           # 连 170（3盘位）
ssh tnas2                                          # 连 171（7盘位）
ssh tnas3                                          # 连 173（2盘位）

# ——— Python ———
py -m pytest                                       # 运行测试（pytest.ini 指向 test_automation/）
```

---

> **最后更新：** 2026-08-04（实际运行脚本验证）
> **对账结论：** 修复前名称/存在性 2704 = 2704；不代表请求内容完全一致，修复后重跑受 Apifox 502 阻塞
> **九维体检：** 修复后 8 个 Sheet 全部 0 问题 ✅
> **storagemanager：** 用户 VSCode 自管，AI 禁止修改
