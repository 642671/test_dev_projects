$kind: http-request
url: "{{baseUrl}}/v2/storage/MountPool/:role/:uuid"
method: POST
headers:
  - key: Content-Type
    value: application/x-www-form-urlencoded
  - key: Cookie
    value: "{{Cookie}}"
    description: 不可为空，用户认证所需cookie
  - key: X-Csrf-Token
    value: "{{X-Csrf-Token}}"
    description: 不可为空，用户认证所需token
pathVariables:
  role: damagedPool
  uuid: "{{corrupt_vg3_uuid }}"
body:
  type: formdata
  content: []
order: 16000
