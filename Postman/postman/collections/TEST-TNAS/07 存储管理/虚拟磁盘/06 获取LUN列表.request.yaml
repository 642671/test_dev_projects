$kind: http-request
url: "{{baseUrl}}/v2/virtualdisk/GetLunList?target=iqn.tnas.terramaster.26719092407"
method: GET
headers:
  - key: Cookie
    value: "{{Cookie}}"
    description: 不可为空，用户认证所需cookie
  - key: X-Csrf-Token
    value: "{{X-Csrf-Token}}"
    description: 不可为空，用户认证所需token
queryParams:
  - key: target
    value: iqn.tnas.terramaster.26719092407
    description: Target名称，文本类型，必填参数需要询问用户给出
settings:
  strictSSL: false
  followRedirects: true
examples: ./.resources/06 获取LUN列表.resources/examples
order: 7000
