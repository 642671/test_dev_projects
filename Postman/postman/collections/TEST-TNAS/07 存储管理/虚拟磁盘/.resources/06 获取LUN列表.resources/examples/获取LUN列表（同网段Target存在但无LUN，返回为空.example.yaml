$kind: http-example
name: 获取LUN列表（同网段Target存在但无LUN，返回为空）
request:
  url: "{{baseUrl}}/v2/virtualdisk/GetLunList?target=iqn.tnas.terramaster.26719092407"
  method: GET
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
  queryParams:
    - key: target
      value: iqn.tnas.terramaster.26719092407
      description: Target名称，文本类型，必填参数需要询问用户给出
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 7000
