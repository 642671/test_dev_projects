$kind: http-request
url: "{{baseUrl}}/v2/disk/benchmark/stop?device={{disk1_device}}"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: Cookie
    value: "{{Cookie}}"
    description: 不可为空，用户认证所需cookie
  - key: X-Csrf-Token
    value: "{{X-Csrf-Token}}"
    description: 不可为空，用户认证所需token
queryParams:
  device: "{{disk1_device}}"
body:
  type: json
  content: "{\r

    \    \"device\": \"{{disk1_device}}\"\r

    }"
settings:
  strictSSL: false
  followRedirects: true
examples: ./.resources/27 停止性能测试.resources/examples
order: 27000
