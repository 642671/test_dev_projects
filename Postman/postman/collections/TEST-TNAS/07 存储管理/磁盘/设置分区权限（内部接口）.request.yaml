$kind: http-request
url: "{{baseUrl}}/v1/part_perms?devtype"
method: PUT
headers:
  - key: Content-Type
    value: application/json
  - key: X-Csrf-Token
    value: "{{X-Csrf-Token}}"
    description: 不可为空，用户认证所需token
queryParams:
  - key: devtype
    value: null
    description: 设备类型：usb=USB设备，其他=硬盘
body:
  type: json
  content: ""
settings:
  strictSSL: false
  followRedirects: true
order: 43000
