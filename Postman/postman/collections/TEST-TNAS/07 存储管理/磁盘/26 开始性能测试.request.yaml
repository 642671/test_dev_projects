$kind: http-request
url: "{{baseUrl}}/v2/disk/benchmark/start?device={{disk1_device}}"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: Cookie
    value: "{{Cookie}}"
    description: 不可为空，用户认证所需cookie
  - key: X-Csrf-Token
    value: "{{X-Csrf-Token}}"
    description: 不可为空，用户认证所需token
queryParams:
  - key: device
    value: "{{disk1_device}}"
    description: 设备路径，如/dev/sda
body:
  type: json
  content: "{\r

    \    \"device\": \"{{disk1_device}}\"\r

    }"
settings:
  strictSSL: false
  followRedirects: true
examples: ./.resources/26 开始性能测试.resources/examples
order: 26000
