$kind: http-example
request:
  url: "{{baseUrl}}/v2/disk/smart_test/schedule/:id/stop"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
  pathVariables:
    - key: id
      value: "{{disk1_smart_id}}"
      description: 任务ID，必填参数需要询问用户给出
    - key: abc
      value: "123"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 7000
