$kind: http-example
request:
  url: "{{baseUrl}}/v2/disk/GetDiskOption"
  method: GET
  headers:
    - key: X-Csrf-Token
      value: "{{expired_X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
    - key: Cookie
      value: "{{expired_Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: Content-Type
      value: application/json
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 3000
