$kind: http-example
name: 正常新增系统盘（系统盘为HDD，选中HDD类型存储池中的磁盘）
request:
  url: "{{baseUrl}}/v2/disk/AddSysDisk"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
    - key: X-Curpass-Token
      value: "{{X-Curpass-Token}}"
  body:
    type: json
    content: |-
      {
          "disk": "{{disk1_device}}"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 10000
