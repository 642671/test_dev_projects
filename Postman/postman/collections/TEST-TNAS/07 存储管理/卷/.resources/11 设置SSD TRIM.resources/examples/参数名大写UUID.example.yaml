$kind: http-example
request:
  url: "{{baseUrl}}/v2/storage/ssd/trim/:uuid"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
  pathVariables:
    - key: UUID
      value: "{{lv0_uuid}}"
    - key: uuid
      value: "{{lv5_uuid}}"
      description: 卷UUID，必填参数需要询问用户给出
  body:
    type: json
    content: |-
      {
          "is_run_immediately": 0,
          "lv_name": "lv0"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 14000
