$kind: http-example
request:
  url: "{{baseUrl}}/v2/storage/volumeOccupyStatus?sads=&sadas=dsadas&uuid={{lv5_uuid}}"
  method: GET
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
  queryParams:
    - key: sads
      value: ""
    - key: sadas
      value: dsadas
    - key: uuid
      value: "{{lv5_uuid}}"
      description: 卷UUID，必填参数需要询问用户给出
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 3000
