$kind: http-example
request:
  url: "{{baseUrl}}/v2/storage/list/volume?type=detail或brief&哈哈哈哈=xcc"
  method: GET
  headers:
    - key: X-Csrf-Token
      value: "{{expired_X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
    - key: Cookie
      value: "{{expired_Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: Content-Type
      value: application/json
  queryParams:
    - key: type
      value: detail或brief
      description: 信息类型：detail=详细信息, brief=简要信息
    - key: 哈哈哈哈
      value: xcc
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 10000
