$kind: http-example
name: 请求体为空对象访问设置阵列同步速度及Bitmap接口
request:
  url: "{{baseUrl}}/v2/storage/SetRaidSpeedOrBitmap"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
  body:
    type: json
    content: "{}"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 8000
