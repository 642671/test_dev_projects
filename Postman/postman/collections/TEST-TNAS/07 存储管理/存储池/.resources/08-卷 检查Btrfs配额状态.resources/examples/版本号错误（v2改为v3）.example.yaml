$kind: http-example
request:
  url: "{{baseUrl}}/v2/storage/CheckQuota?raid={{vg0_raid}}&mntPath={{lv0_mnt_path}}"
  method: GET
  headers:
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
  queryParams:
    - key: raid
      value: "{{vg0_raid}}"
    - key: mntPath
      value: "{{lv0_mnt_path}}"
      description: 挂载路径，必填参数需要询问用户给出
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 16000
