$kind: http-example
request:
  url: "{{baseUrl}}/v2/extStorageDevice/IsFormating?name=哈哈哈哈"
  method: GET
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
  queryParams:
    - key: name
      value: 哈哈哈哈
      description: 设备名称，如sda，必填参数需要询问用户给出
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 5000
