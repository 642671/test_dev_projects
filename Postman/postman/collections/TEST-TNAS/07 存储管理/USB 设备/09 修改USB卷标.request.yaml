$kind: http-request
url: "{{baseUrl}}/v2/extStorageDevice/EditUsbLabel"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: Cookie
    value: "{{Cookie}}"
    description: 不可为空，用户认证所需cookie
  - key: X-Csrf-Token
    value: "{{X-Csrf-Token}}"
    description: 不可为空，用户认证所需token
body:
  type: json
  content: "{\r

    \    \"device_name\": \"{{usb1_device}}\",\r

    \    \"label\": \"2\"\r

    }"
settings:
  strictSSL: false
  followRedirects: true
examples: ./.resources/09 修改USB卷标.resources/examples
order: 9000
