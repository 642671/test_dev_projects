$kind: http-example
name: 处于同步中、修复中、更换磁盘中、配置中、迁移中的冗余阵列不展示
request:
  url: "{{baseUrl}}/v2/hotsparedisk/GetRaidHotSpare"
  method: GET
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 19000
