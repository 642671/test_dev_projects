$kind: http-example
request:
  url: "{{baseUrl}}/v2/lvmCache/CreateCacheLv"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{NormalUserCookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{NormalUserCsrfToken}}"
      description: 不可为空，用户认证所需token
    - key: X-Curpass-Token
      value: "{{NormalUser_X-Curpass-Token}}"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 42000
