$kind: http-example
name: 获取最大剩余可设置缓存大小（传入未挂载卷失败）
request:
  url: "{{baseUrl}}/v2/lvmCache/GetMaxResidueCacheSize?volumeUUID={{unmount_lv_uuid}}"
  method: GET
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
  queryParams:
    - key: volumeUUID
      value: "{{unmount_lv_uuid}}"
      description: 卷UUID，文本类型，必填参数需要询问用户给出
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 4000
