$kind: http-example
name: 获取空闲SSD列表（存在SSD是系统盘则不返回该SSD）
request:
  url: "{{baseUrl}}/v2/lvmCache/FreeSSDs"
  method: GET
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 3000
