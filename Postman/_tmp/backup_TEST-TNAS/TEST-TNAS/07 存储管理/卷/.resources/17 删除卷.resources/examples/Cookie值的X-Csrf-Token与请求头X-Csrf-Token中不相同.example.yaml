$kind: http-example
request:
  url: "{{baseUrl}}/v2/storage/delete/volume/:uuid?uuid={{lv0_uuid}}"
  method: DELETE
  headers:
    - key: X-CurPass-Token
      value: "{{X-CurPass-Token}}"
      description: 不可为空，密码认证密文
    - key: X-Csrf-Token
      value: "{{wrong_X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
    - key: Content-Type
      value: application/x-www-form-urlencoded
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
  queryParams:
    uuid: "{{lv0_uuid}}"
  pathVariables:
    uuid: ""
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 26000
