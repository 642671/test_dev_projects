$kind: http-example
request:
  url: "{{baseUrl}}/v2/storage/EditVolumeRemindConfig?uuid={{lv0_uuid}}&dasd=sada"
  method: POST
  headers:
    - key: X-Csrf-Token
      value: "{{NormalUserCsrfToken}}"
      description: 不可为空，用户认证所需token
    - key: Cookie
      value: "{{NormalUserCookie}}"
      description: 不可为空，用户认证所需cookie
    - key: Content-Type
      value: application/json
  queryParams:
    uuid: "{{lv0_uuid}}"
    dasd: sada
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 16000
