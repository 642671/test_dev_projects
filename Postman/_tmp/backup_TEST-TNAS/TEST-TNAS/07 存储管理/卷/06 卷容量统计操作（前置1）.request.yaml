$kind: http-request
url: "{{baseUrl}}/v2/storage/volumeOccupyAction"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: Cookie
    value: "{{Cookie}}"
    description: 不可为空，用户认证所需cookie
  - key: X-Csrf-Token
    value: "{{X-Csrf-Token}}"
    description: 不可为空，用户认证所需token
body:
  type: json
  content: "{\r

    \    \"uuid\": \"{{lv0_uuid}}\",\r

    \    \"action\": \"stop\"\r

    }"
settings:
  strictSSL: false
  followRedirects: true
examples: ./.resources/06 卷容量统计操作（前置1）.resources/examples
order: 6000
