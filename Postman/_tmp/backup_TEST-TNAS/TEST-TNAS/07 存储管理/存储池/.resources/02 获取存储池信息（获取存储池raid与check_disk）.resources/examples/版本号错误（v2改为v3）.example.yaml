$kind: http-example
request:
  url: "{{baseUrl}}/v2/storage/edit/pool/:uuid?raid={{vg0_raid}}"
  method: GET
  headers:
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
  queryParams:
    raid: "{{vg0_raid}}"
  pathVariables:
    uuid: ""
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 16000
