$kind: http-example
name: 正常格式化整个磁盘（已挂载的ext4设备为ext4）成功
request:
  url: "{{baseUrl}}/v2/extStorageDevice/FormattingDevice"
  method: POST
  headers:
    - key: Content-Type
      value: application/x-www-form-urlencoded
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
    - key: X-Curpass-Token
      value: "{{X-Curpass-Token}}"
  body:
    type: urlencoded
    content:
      filesystem: ext4
      name: "{{usb1_name}}"
      type: usb
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 9000
