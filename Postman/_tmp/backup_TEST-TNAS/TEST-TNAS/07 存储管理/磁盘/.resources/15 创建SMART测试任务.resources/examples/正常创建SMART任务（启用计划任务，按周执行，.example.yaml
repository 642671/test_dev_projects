$kind: http-example
name: 正常创建SMART任务（启用计划任务，按周执行，周一三五）
request:
  url: "{{baseUrl}}/v2/disk/smart_test/schedule"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: X-Csrf-Token
      value: "{{X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
  body:
    type: json
    content: |-
      {
          "name": "{{disk1_model}}",
          "serial": "{{disk1_serial}}",
          "test_type": 0,
          "disks": "{{disk1_device}}",
          "enabled": true,
          "run_immediately": false,
          "exception_warn": true,
          "schedule": {
              "cycle_type": "WeeksRange",
              "cycle_weeks_range": "1,3,5",
              "cycle_date": "",
              "schedule_time": "",
              "frequency_type": "Disabled",
              "frequency_value": 0,
              "hour": 3,
              "minute": 0
          }
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 17000
