$kind: http-example
request:
  url: "{{baseUrl}}/v2/disk/GetDiskStatus"
  method: GET
  headers:
    - key: X-Csrf-Token
      value: "{{wrong_X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
    - key: Content-Type
      value: application/json
    - key: Cookie
      value: "{{Cookie}}"
      description: 不可为空，用户认证所需cookie
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 8000
