$kind: http-example
request:
  url: "{{baseUrl}}/v2/disk/DelSysDisk"
  method: DELETE
  headers:
    - key: X-Csrf-Token
      value: "{{expired_X-Csrf-Token}}"
      description: 不可为空，用户认证所需token
    - key: Cookie
      value: "{{expired_Cookie}}"
      description: 不可为空，用户认证所需cookie
    - key: Content-Type
      value: application/json
    - key: X-Curpass-Token
      value: "{{X-Curpass-Token}}"
      description: 不可为空，密码认证所以密文
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 6000
