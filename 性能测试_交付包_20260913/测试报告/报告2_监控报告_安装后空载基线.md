# MultimediaServer · 应用资源监控报告

> **CPU 口径**：单核口径（100% = 1 个核；4 核跑满 = 400%）　·　**采样间隔**：1.0s　·　**方法**：进程树资源监控（父进程 + 全部子孙进程）

## 一、结论概览

| 总判定 | 通过 | 待确认 | 不通过 | 未采集 |
|---|---|---|---|---|
| **✓ 通过** | 13 | 0 | 0 | 0 |

### 问题清单（逐项结论）

> 一眼看清「有没有问题」—— 不用翻后面的表。

| 关注点 | 结论 | 依据 |
|---|---|---|
| **内存泄漏** | **✅ 无** | 各窗口 private_Dirty 净变化最大 **+0.09 MB**（8.30 → 8.39），单调度 0.010 —— **均无单调爬升**（判据：净变化 >5% 且单调度 >0.6 才算疑泄漏）；⚠️ **本报告窗口仅 6 分钟，只能排除「快速泄漏」，慢泄漏需长稳（≥8 小时）才能判** |
| **单进程内存峰值** | **✅ 无** | 峰值 **8.4 MB**（private_Dirty），红线 1024 MB —— 未超线 |
| **CPU 瓶颈** | **✅ 无** | 峰值 0.01%，未超阈值 70 |
| **磁盘 I/O 瓶颈** | **✅ 无** | 进程磁盘读写峰值 **读 0.00 / 写 0.00 MB/s**（本口径无 `%util`；阈值参考：`%util` ≤80%） |
| **网络带宽瓶颈** | **✅ 无** | 发送峰值 0.04 MB/s（0 Mbps），占用很低 |
| **错误率** | **– 未采集** | 本次为非请求-响应类负载，无错误率口径（以拉流成功/失败代替） |

## 二、监控概况

| 项 | 值 |
|---|---|
| 场景数 | 1（MultimediaServer） |
| 样本数 | 387 |
| 坏行剔除 | 0 |
| 采集窗口 | 2026-09-13 15:34:15 → 2026-09-13 15:40:45 |

| 场景 | 进程组 | 样本 | 实际窗口 |
|---|---|---|---|
| MultimediaServer | P4_app_fixed | 387 | 2026-09-13 15:34:15 → 2026-09-13 15:40:45 |

## 三、指标统计

| 指标 | 单位 | MultimediaServer 均值 | 阈值 | 判定 |
|---|---|---|---|---|
| CPU（单核口径） | % | 0.01 | 70 | ✓ 通过 |
| PSS | MB | 31.50 | — | ✓ 通过 |
| RSS | MB | 48.46 | — | ✓ 通过 |
| private_Dirty | MB | 8.36 | — | ✓ 通过 |
| RssAnon | MB | 8.36 | — | ✓ 通过 |
| RssFile（页缓存） | MB | 40.10 | — | ✓ 通过 |
| swap | MB | 0 | — | ✓ 通过 |
| 磁盘读 | MB/s | 0 | — | ✓ 通过 |
| 磁盘写 | MB/s | 0 | — | ✓ 通过 |
| 网络发送（整机） | MB/s | 0 | 30 | ✓ 通过 |
| 网络接收（整机） | MB/s | 0.01 | — | ✓ 通过 |
| 进程树规模 | 个 | 2 | — | ✓ 通过 |
| 单进程内存峰值（private_Dirty） | MB | 8.39 | 1024 | ✓ 通过 |

> 阈值来源：**默认参考值（业界参考，非业务验收标准）**。要按项目验收标准判定，用 `--thresholds <json>` 覆盖。

## 四、峰值与发生时刻

> 峰值必须带时刻 —— 否则会把一次性尖峰（如批量写入收尾、缓存刷盘）误读成持续压力。

| 场景 | 进程组 | 指标 | 峰值 | 发生时刻 |
|---|---|---|---|---|
| MultimediaServer | P4_app_fixed | CPU（单核口径） | 1% | 2026-09-13 15:35:38 |
| MultimediaServer | P4_app_fixed | PSS | 31.53MB | 2026-09-13 15:36:36 |
| MultimediaServer | P4_app_fixed | RSS | 48.49MB | 2026-09-13 15:36:36 |
| MultimediaServer | P4_app_fixed | private_Dirty | 8.39MB | 2026-09-13 15:36:36 |
| MultimediaServer | P4_app_fixed | RssAnon | 8.39MB | 2026-09-13 15:36:36 |
| MultimediaServer | P4_app_fixed | RssFile（页缓存） | 40.10MB | 2026-09-13 15:34:15 |
| MultimediaServer | P4_app_fixed | swap | 0MB | 2026-09-13 15:34:15 |
| MultimediaServer | P4_app_fixed | 磁盘读 | 0MB/s | 2026-09-13 15:34:15 |
| MultimediaServer | P4_app_fixed | 磁盘写 | 0MB/s | 2026-09-13 15:34:15 |
| MultimediaServer | P4_app_fixed | 网络发送（整机） | 0.04MB/s | 2026-09-13 15:36:36 |
| MultimediaServer | P4_app_fixed | 网络接收（整机） | 0.02MB/s | 2026-09-13 15:34:33 |
| MultimediaServer | P4_app_fixed | 进程树规模 | 2个 | 2026-09-13 15:34:15 |

## 五、使用期间 vs 空闲期间

> 判定口径：**CPU > 2% 或 磁盘读写合计 > 0.5 MB/s 或 网络收发合计 > 0.5 MB/s = 使用**。

| 进程组 | 使用样本 | 占比 | 使用期CPU均值 | 空闲样本 | 占比 | 空闲期CPU均值 | 空闲期PSS均值 |
|---|---|---|---|---|---|---|---|
| P4_app_fixed | 0 | 0.0% | 0 | 387 | 100.0% | 0.01 | — |

## 七、内存回落与泄漏判定

**未发现内存泄漏迹象**

| 场景 | 进程 | 首段均值 | 末段均值 | 变化 | 单调度 | 判定 |
|---|---|---|---|---|---|---|
| MultimediaServer | P4_app_fixed | 8.30 MB | 8.39 MB | 0.09 MB | 0.01 | 无增长 |

> 判据：**末段均值 − 首段均值**（各取 1/4 样本，避免单点噪声）配合**单调度**（相邻递增占比）。空闲期内存不归零属正常设计，只要**无单调爬升**即可判无泄漏。

## 八、采集覆盖度与未测项

> **采不到 ≠ 达标。** 下列指标本次没有采到，报告里一律显示「未采集」而**不填 0**。

| 项 | 状态 | 说明 |
|---|---|---|
| 错误率 / 成功率 | ❌ 未采集 | DLNA 直传无应用层请求统计口径，本次以 curl 的 HTTP 状态码代替：3 轮共 30 路拉流全部 200，无失败 |
| 响应时间 / P95 / P99 | ❌ 未采集 | 推流为长连接字节流，无「请求-响应」语义，不适用 RT 指标（见 references/metrics.md 口径说明） |
| 数据库连接数 / 慢查询 / 死锁 | ❌ 未采集 | MiniDLNA 使用本地 SQLite 索引库，无独立数据库连接池 |
| Full GC 频率与时长 | ❌ 未采集 | MultimediaServer / mediaindex 均为 C 实现，非 JVM 应用，无 GC 指标 |
| 前端指标 FCP/LCP/FID/CLS | ❌ 未采集 | 本次为服务端资源与推流性能测试，未采集浏览器端指标 |
| 磁盘 %util / await | ⚠️ 口径降级 | 设备未安装 sysstat，无 iostat；已用 /proc/diskstats 折算读写速率与整机 %iowait 代替 |
| P0 未安装态基线 | ⚠️ 口径降级 | 窗口尾部（15:03 之后）可能已含安装包下载/解包活动，非绝对空载 |
| P2 安装后空载基线（15:06-15:11 段） | ⚠️ 口径降级 | 该段由 v1 采集器采集，进程树多计了采集器自身 1 个进程；15:34 起的 P4 段为修复版，口径干净 |

## 九、口径声明

- **网络列为整机口径**：Linux 没有逐进程网络字节计数（`/proc/<pid>/net/dev` 属于整个网络命名空间），因此该列取自整机数据，**不可归因给单个应用/功能**。
- **内存判压力请用 `private_Dirty` 或 `PSS`，不要用 RSS**：RSS 含 `RssFile`（mmap 文件页缓存，内核可随时回收），会被严重放大。
- **CPU 为单核口径**：100% = 1 个核，4 核跑满 = 400%。
- **评审红线（硬性）**：**单个进程的内存峰值超过 1 GB** —— 超过即需团队讨论评估，不是「达标/不达标」的二元判定。判定口径用 `private_Dirty`（真实匿名内存），**不用 RSS**（RSS 含可回收的 mmap 页缓存，会把页缓存误判成占用）。
- **其余阈值为业界参考值**（CPU ≤70%、`%util` ≤80%、错误率 ≤0.2% 等），**非业务验收标准**，**需各项目组结合自身业务具体讨论定夺**。本报告判定用的是**达标线**口径；源材料另有**警戒值**口径（CPU 75%~80%、磁盘繁忙率 <70%），两者含义不同，**勿混用**。

---

 *由 `性能测试` skill 的 `perf_report.py` 于 2026-09-13 17:22:50 生成。指标口径见 `references/metrics.md`，方法学陷阱见 `references/methodology-pitfalls.md`。*
