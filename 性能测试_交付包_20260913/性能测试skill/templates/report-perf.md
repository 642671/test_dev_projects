> **本模板供「手写」报告使用。** 有采集结果（`*_stats.json` / `.jtl` / k6 summary）时，
> 请用 `scripts/perf_report.py --kind perf` **自动生成** —— 结构与本模板一致，
> 且自带阈值判定、放大倍数、内联 SVG 趋势图与自包含 HTML。

# {{TITLE}}

<!-- 标题写法：`<应用名> · 性能测试报告`，例：MultimediaServer · 性能测试报告 -->

> **元信息**：设备 **{{DEVICE}}**　·　**CPU 口径：单核口径（100% = 1 个核；4 核跑满 = 400%）**　·　采样间隔 **{{INTERVAL_S}}s**　·　方法 **进程树资源监控 + 负载侧并发**　·　生成时间 **{{GENERATED_AT}}**

<!-- 本体裁的对比轴是「**同一进程**：空闲 vs 负载」，至少要有 2~3 个场景 ——
     单场景出不来「放大倍数」，也说明不了「负载带来多少开销」。 -->

---

## 一、监控概况

<!-- 先自证「负载真压上了」：并发路数 / TCP 连接数 / 累计传输字节。
     没有这一节，读者无法区分「测出来开销低」和「压根没压上」。 -->

| 项 | 值 |
|---|---|
| 应用 / 进程组 | MultimediaServer / mediaindex |
| 场景数 | 2（空闲、4 路转码） |
| 采集窗口 | 空闲 2026-09-13 10:00:00 → 10:04:59　·　负载 2026-09-13 10:20:00 → 10:29:59 |
| 样本数 | 空闲 300　·　负载 600 |
| 坏行剔除 | 0 行 |
| 采样间隔 | 1 s |

**负载真实性证据**（**必给**）

| 项 | 值 |
|---|---|
| 客户端并发路数 | 4 路 |
| 服务端 TCP 连接数（峰值） | 6 |
| 累计传输字节 | 发送 1.8 MB / 接收 5.4 MB |
| 媒资素材 | 4 路 1080p 转码，同一批素材（**两次测试素材必须一致**） |

---

## 二、指标统计表

<!-- 列序固定为 **均值 / 峰值 / P95 / P99** —— 三种体裁里只有 perf 强制四列，别省 P99。
     峰值旁边必须能查到发生时刻（见下一节）。 -->

| 指标 | 单位 | 空闲 均值 | 负载 均值 | 空闲 峰值 | 负载 峰值 | 负载 P95 | 负载 P99 | 阈值 | 判定 |
|---|---|---|---|---|---|---|---|---|---|
| CPU（单核口径） | % | 0.4 | 297.75 | 0.98 | 341.2 | 331.0 | 338.5 | ≤70 | ✗ 不通过 |
| PSS | MB | 23.5 | 97.4 | 23.9 | 104.6 | 101.2 | 103.4 | — | – 未采集 |
| private_Dirty | MB | 8.72 | 41.2 | 8.81 | 45.8 | 44.1 | 45.2 | — | – 未采集 |
| RSS | MB | 34.4 | 128.6 | 34.8 | 141.2 | 138.0 | 140.1 | — | – 未采集 |
| RssFile（页缓存） | MB | 25.7 | 58.1 | 25.7 | 66.0 | 64.2 | 65.5 | — | – 未采集 |
| 磁盘写 | MB/s | 0.01 | 12.6 | 0.30 | 31.4 | 28.8 | 30.9 | — | – 未采集 |
| 网络发送（整机） | MB/s | 0.01 | 0.42 | 0.02 | 1.10 | 1.02 | 1.08 | ≤30 | ✓ 通过 |
| 进程树规模 | 个 | 4 | 11 | 4 | 13 | 13 | 13 | — | – 未采集 |

<!-- 上表示例数据取自真实量级。注意两件事：
     ① 判定列「– 未采集」不是「通过」—— 有阈值才判，没阈值只展示。
     ② CPU 297.75% 在**单核口径**下等于「约 3 个核在跑」，不是「超标 300%」。 -->

> 阈值来源：**默认参考值（业界参考，非业务验收标准）**。要按项目验收标准判定，
> 用 `perf_report.py --thresholds <json>` 覆盖。

---

## 三、分组趋势折线图

<!-- 四组图：CPU / 内存 / 磁盘 / 网络，按场景分组（空闲一条、负载一条）。
     手写时贴图；用 perf_report.py 生成时是内联 SVG，且缺采样点会**断线分段**——
     断口表示「这一秒没采到」，不是 0。 -->

**CPU（单核口径，%）**

```
（在此贴图）
```

**内存：PSS / private_Dirty / RSS（MB）**

```
（在此贴图）
```

**磁盘读写（MB/s）**

```
（在此贴图）
```

**网络收发（整机口径，MB/s）**

```
（在此贴图）
```

---

## 四、放大倍数（空闲 → 负载）

<!-- 这一节回答「负载带来多少开销」。空载均值为 0 时倍数是无穷，写「∞」不要写数字。 -->

| 指标 | 空闲均值 | 负载均值 | 放大倍数 |
|---|---|---|---|
| CPU（单核口径） | 0.4 % | 297.75 % | **744.4×** |
| PSS | 23.5 MB | 97.4 MB | **4.1×** |
| private_Dirty | 8.72 MB | 41.2 MB | **4.7×** |
| 磁盘写 | 0.01 MB/s | 12.6 MB/s | **1260×** |
| 进程树规模 | 4 个 | 11 个 | **2.8×** |

> 放大倍数用于说明**量级差异**；窗口不同、素材不同时**不宜作精确比较**。

---

## 五、内存回落与泄漏判定

<!-- 有 churn / 长稳数据时才出这一节。判据写在下面，别只给结论。 -->

| 场景 | 进程 | 首段均值 | 末段均值 | 变化 | 单调度 | 判定 |
|---|---|---|---|---|---|---|
| 4 路转码 | mediaindex | 41.02 MB | 41.18 MB | +0.16 MB | 0.48 | 无增长 |

> 判据：**末段均值 − 首段均值**（各取 1/4 样本，避免单点噪声）配合**单调度**
> （相邻递增占比）。空闲期内存不归零属正常设计，只要**无单调爬升**即可判无泄漏。

---

## 六、结论与优化建议

<!-- 瓶颈定位的结论句式见 references/analysis-playbook.md。
     注意：结论要落到「哪一维先到顶」，而不是罗列所有指标。 -->

**瓶颈定位**：{{BOTTLENECK}}

**依据**

- CPU 负载均值 297.75%（4 核上限 400%），已用掉 **74.4%** —— 逼近上限，是当前第一约束。
- 磁盘写峰值 31.4 MB/s、网络 1.10 MB/s，均远未到量级上限，不是瓶颈。
- 内存 PSS 4.1×、private_Dirty 4.7×，随负载线性放大，无异常。

**建议**

1. {{SUGGESTION_1}}
2. {{SUGGESTION_2}}

---

## 七、采集覆盖度与未测项

<!-- 必带收尾章之一。三态而非两态：✅ 已采 / ⚠️ 口径降级 / ❌ 未采集。 -->

| 项 | 状态 | 说明 |
|---|---|---|
| CPU / PSS / private_Dirty / 磁盘 / 进程树 | ✅ 已采 | 进程树口径，逐秒 |
| 网络收发 | ⚠️ 口径降级 | **整机口径**，不可归因单应用 |
| 响应时间 / TPS / 错误率 | ❌ 未采集 | 本次是资源监控口径；接口侧指标需用 k6/JMeter 另采 |
| Full GC 次数 | ❌ 未采集 | JRE 未开 GC 日志 |

**统计**：共 4 项 —— ✅1 / ⚠️1 / ❌2。

---

## 八、口径声明

<!-- 必带收尾章之二。 -->

- **网络列为整机口径**：Linux 没有逐进程网络字节计数（`/proc/<pid>/net/dev` 属于整个网络命名空间），
  该列取自整机数据，**不可归因给单个应用 / 功能**。
- **内存判压力请用 `private_Dirty` 或 `PSS`，不要用 `RSS`**：RSS 含 `RssFile`
  （mmap 文件页缓存，内核可随时回收），会被严重放大。
- **CPU 为单核口径**：100% = 1 个核，4 核跑满 = 400%。
- **阈值来源为默认参考值**（业界参考），**非业务验收标准**。

---

<!-- 相关文档：references/analysis-playbook.md（瓶颈决策树与结论话术）、
     method-pitfalls 见 methodology-pitfalls.md、报告骨架见 report-skeletons.md -->
