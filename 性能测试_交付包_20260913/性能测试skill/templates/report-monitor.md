> **本模板供「手写」报告使用。** 有采集结果（`*_stats.json` / `results.json` / `.jtl` / k6 summary）时，
> 请用 `scripts/perf_report.py --kind monitor` **自动生成** —— 结构与本模板一致，
> 且自带阈值判定、内联 SVG 趋势图与自包含 HTML。

# {{TITLE}}

<!-- 标题写法：`<应用名> · 应用资源监控报告`，例：MultimediaServer · 应用资源监控报告 -->

> **元信息**：设备 **{{DEVICE}}**　·　**CPU 口径：单核口径（100% = 1 个核；4 核跑满 = 400%）**　·　采样间隔 **{{INTERVAL_S}}s**　·　方法 **进程树资源监控（父进程 + 全部子孙进程）**　·　生成时间 **{{GENERATED_AT}}**

<!-- 元信息五项都是硬要求：CPU 口径写不清，400% 会被读成「超标 4 倍」；
     采样间隔写不清，别人无法判断曲线上的抖动是真实负载还是采样太粗。 -->

---

## 总结

<!-- 这一节是给「只看结论的人」的：一句话 + 一张数字表 + 几条要点 + 建议。
     不要写成流水账，也不要只放图不给数。 -->

**一句话结论**：{{SUMMARY}}

| 项 | 数值 | 口径 / 备注 |
|---|---|---|
| 进程树规模 | 4 个进程 | 父 + 全部子孙 |
| CPU（使用期均值） | 0.62 % | 单核口径 |
| PSS（使用期均值） | 23.8 MB | 共享页按比例分摊 |
| private_Dirty（使用期均值） | 8.79 MB | **判内存压力看这个** |
| RSS（使用期均值） | 34.4 MB | 含 RssFile 页缓存，会虚高 |
| 磁盘写（峰值） | 0.30 MB/s | 发生时刻 10:00:02 |
| 网络（整机口径，收发合计） | 0.05 MB/s | **不可归因单应用** |

<!-- 上表一行示例数据取自真实采集（MultimediaServer 空载）。替换成你的数。
     每行都要能回答「这个数是怎么来的」。 -->

**要点**

- 使用期占 **39.3%** 的窗口，空闲期占 60.7% —— 说明窗口里确实有负载，不是空跑。
- 开销**长在子进程上**：只盯常驻父进程会漏掉大部分开销（实测有漏掉 77% 的反例）。
- 内存无单调爬升（单调度 0.47，末段 − 首段 = −0.02 MB），**未见泄漏迹象**。
- `swap` 全程 0 MB，无页交换。

**建议**

1. {{SUGGESTION_1}}
2. {{SUGGESTION_2}}

---

## 一、监控概况

<!-- 先交代「数据可不可信」，再给结论。坏行剔除数与完整率必须出现。 -->

| 项 | 值 |
|---|---|
| 应用 / 进程组 | MultimediaServer / mediaindex |
| 采集窗口 | 2026-09-13 10:00:00 → 2026-09-13 10:04:59 |
| 样本数 | 300（1 秒采样） |
| 坏行剔除 | 0 行 |
| 完整率 | 100 %（300 / 300） |

**进程构成明细**（父 + 全部子孙，**必给** —— 只给合计会掩盖「开销长在子进程上」）

| 进程 | PID | PSS（峰值 / 均值） | private_Dirty（峰值 / 均值） | CPU 均值 | 说明 |
|---|---|---|---|---|---|
| mediaindex | 1234 | 23.9 / 23.5 MB | 8.81 / 8.72 MB | 0.41 % | 常驻父进程 |
| mediaindex-worker | 1251 | 5.2 / 5.1 MB | 2.10 / 2.05 MB | 0.18 % | 子进程（转码 / 索引） |
| **合计** | — | **29.1 / 28.6 MB** | **10.91 / 10.77 MB** | **0.59 %** | 报告以合计为准 |

---

## 二、使用期 vs 空闲期

<!-- 判定口径必须写在正文里，否则读者不知道「使用」是怎么切的。 -->

> 判定口径：**CPU > 2% 或 磁盘读写合计 > 0.5 MB/s 或 网络收发合计 > 0.5 MB/s = 使用**。

| 项 | 使用期 | 空闲期 |
|---|---|---|
| 样本数 | 118 | 182 |
| 占比 | 39.3 % | 60.7 % |
| CPU 均值 | 0.62 % | 0.04 % |
| PSS 均值 | 23.8 MB | 23.4 MB |
| private_Dirty 均值 | 8.79 MB | 8.70 MB |
| 磁盘写均值 | 0.14 MB/s | 0.00 MB/s |

---

## 三、汇总曲线

<!-- 手写时贴手工截的图（iostat / pidstat / 采集器导出的折线）。
     截图必须带**坐标轴与时间刻度**，否则峰值会被误读成持续压力。
     用 perf_report.py 生成时这里是内联 SVG（断网可看、零外部请求）。 -->

**内存（PSS / private_Dirty / RSS，MB）**

```
（在此贴图，或粘贴抽稀后的时间序列）
```

**CPU（单核口径，%）**

```
（在此贴图）
```

**磁盘读写（MB/s）**

```
（在此贴图）
```

---

## 四、结论

<!-- 结论章必须给**原始 CSV 落点** —— 读者要能自己回去核数。
     写了结论却不给数据来源，等于让下一个人把整件事重做一遍。 -->

{{CONCLUSION}}

> **原始数据落点**
>
> - `P2_MultimediaServer_mediaindex.csv` —— 进程树口径，300 行，1 秒采样
> - `P0_host.csv` / `P5_host.csv` —— 安装前 / 卸载后整机基线（对照用）
> - `P2_stats.json` —— 本节所有统计量的来源（`perf_analyze.py` 产出）

---

## 五、采集覆盖度与未测项

<!-- 必带收尾章之一。三态而非两态：✅ 已采 / ⚠️ 口径降级 / ❌ 未采集。
     红线：**采不到 ≠ 达标**，**绝不拿 0 冒充实测值**（0 会被读成「实测值为 0」）。 -->

| 项 | 状态 | 说明 |
|---|---|---|
| CPU / PSS / private_Dirty / RSS | ✅ 已采 | 进程树口径，逐秒 |
| 磁盘读写 | ✅ 已采 | /proc/<pid>/io，已扣除 cancelled_write_bytes |
| 网络收发 | ⚠️ 口径降级 | **整机口径**，不可归因单应用（Linux 无逐进程网络计数） |
| Full GC 次数 | ❌ 未采集 | JRE 未开 GC 日志；如需请在应用侧打开后重采 |
| 磁盘 %util / %iowait | ❌ 未采集 | 本次只采了 MB/s 速率，未采利用率 |

**统计**：共 5 项 —— ✅2 / ⚠️1 / ❌2。

---

## 六、口径声明

<!-- 必带收尾章之二。四条固定声明，一个字都别省。 -->

- **网络列为整机口径**：Linux 没有逐进程网络字节计数（`/proc/<pid>/net/dev` 属于整个网络命名空间），
  该列取自整机数据，**不可归因给单个应用 / 功能**。
- **内存判压力请用 `private_Dirty` 或 `PSS`，不要用 `RSS`**：RSS 含 `RssFile`
  （mmap 文件页缓存，内核可随时回收），会被严重放大。
- **CPU 为单核口径**：100% = 1 个核，4 核跑满 = 400%。
- **阈值来源为默认参考值**（业界参考），**非业务验收标准**。

---

<!-- 相关文档：references/metrics.md（口径与阈值）、memory-accounting.md（内存五件套）、
     methodology-pitfalls.md（十八条陷阱）、app-lifecycle.md（P0~P5 各阶段数据对应哪一章） -->
